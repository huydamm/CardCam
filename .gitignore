# training data — too big for git, regenerate via train script
samples/
model/*.keras
model/*.png
# card_model.tflite (2.8MB) IS committed so others can run without retraining

# python
__pycache__/
*.pyc

# editor/tooling
.claude/
